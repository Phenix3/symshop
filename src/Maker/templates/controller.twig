<?php declare(strict_types=1);

{% if api %}
namespace App\Api\Controller{{ namespace }};
{% else %}
namespace App\Controller{{ namespace }};
{% endif %}

use App\Controller\AbstractController;
use Symfony\Component\HttpFoundation\{% if api %}JsonResponse{% else %}Response{% endif %};
use Symfony\Component\Routing\Annotation\Route;

class {{ class_name }} extends AbstractController
{
    /**
     * @Route("/demo", name="demo")
     */
    public function index(): {% if api %}JsonResponse{% else %}Response{% endif %}
    {

    }
}
